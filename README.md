# UNVRS Hub Swift

Riscrittura nativa macOS (SwiftUI + AppKit) di UNVRS Hub.
Stesso Supabase della versione Tauri (`~/Library/Application Support/dev.unvrslabs.hub/config.json`).

- `swift run` per avviare in dev
- `scripts/bundle.sh` per creare "UNVRS Hub Swift.app"
